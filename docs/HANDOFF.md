# 红人推广工作台交接文档

> **唯一现役交接版本**
>
> 更新时间：2026-09-02。
>
> 当前已提交基线：`main` / `5151fcb3117b09152b076e22a76577d1b3381717`，提交说明为 `更新优化小bug`，已包含双邮箱邮件正文隔离与编辑区防护。本地 `origin/main` 与 HEAD 一致；本轮通过 GitHub 接口直接核实远端 main 也为该提交。该提交在本轮收尾前已存在，不是本轮代理提交。
> 源码版本仍为 `1.2.5`；GitHub 上该提交的 Vercel 状态为 success。正式域名公开页返回 200，但其发布指向和登录后真实流程仍未独立验收。
> 本轮 localhost 与 127.0.0.1 的 5000、5001 端口均连接拒绝；仓库现有 .next 仍是旧构建，不能直接启动它并当作当前修复验收。本轮不启停服务、不覆盖构建。
> 最近一次实现验证：类型检查、280 项逻辑测试通过；ESLint 0 error、7 个既有 warning；最终隔离生产构建通过，41 个生成项。双邮箱模拟布局及组件浏览器验证详见第 4 节。
> 用户已说明此前功能做过真实验收；此次正文隔离的真实 Gmail/腾讯邮件、真实中文输入法和跨浏览器验收仍待完成，不把模拟验收当作线上验收。
> 本次洁癖只同步 README 和本文件，不提交、推送、部署、改业务代码或清理现场。
>
> 根目录 `HANDOFF.md`、`PROJECT_HANDOFF.md`、`TODO.md` 与 `docs/AI_HANDOFF.md` 仅为历史入口，不代表当前状态。

## 0. 新对话接手顺序

1. 完整阅读根目录 `AGENTS.md` 和本文件。
2. 读取当前分支、HEAD、origin/main 和最近 5 个提交；用明确的安全路径检查差异、未跟踪文件并保留既有修改。不要扫描受保护 `.codex-tmp` 或外部 worktree；本机宽范围 Git status 即使带排除项也可能探测受保护目录。
3. 根据用户的新需求阅读真实入口代码；不要只依据旧截图、提交说明或历史交接推断。
4. 修改前先用中文说明业务目标、涉及模块、影响范围和最小实现方案。
5. Gmail/腾讯草稿与发送、飞书写回、字段结构、密钥和部署仍属于高风险动作；必须保留现有人工确认边界。

## 1. 项目定位与核心入口

这是面向跨境电商海外红人推广专员的桌面 CRM 工作台，覆盖：YouTube 红人发现、
飞书双表建档、Gmail/腾讯企业邮箱、开发信与 Follow Up、AI 翻译/起草、每日待办、工作日历、
合作履约跟进和邮件告知。AI 负责辅助判断和生成，真实外部动作由用户确认。

主要入口：

- 应用壳与导航：`src/app/page.tsx`
- 红人开发台：`src/components/creator-prospecting-page.tsx`
- 开发信 Follow Up：`src/components/creator-prospecting/outreach-follow-up-tab.tsx`
- Gmail：`src/components/gmail-page.tsx`、`email-composer.tsx`、`new-email-composer.tsx`
- 双邮箱账号与切换：`src/components/mail-account-provider.tsx`、`mail-account-switcher.tsx`、`mail-account-settings.tsx`
- 腾讯企业邮箱：`src/components/tencent-exmail-page.tsx`、`src/app/api/mail/**`、`src/lib/tencent-exmail-messages.ts`
- 通用邮件身份/绑定/草稿：`src/lib/mail-accounts.ts`、`mail-account-bindings.ts`、`mail-conversation.ts`、`mail-draft-edit.ts`
- 每日待办：`src/components/todo-board.tsx`、`src/lib/use-daily-gmail-todos.ts`
- 工作日历：`src/components/work-calendar.tsx`、`src/lib/local-date.ts`
- 合作项目：`src/components/cooperation-projects-page.tsx`、`src/lib/cooperation-projects.ts`
- 飞书：`src/lib/feishu-record-index.ts`、`feishu-batch.ts`、`feishu-mapping.ts`
- 飞书成员应用授权：`src/components/feishu-settings.tsx`、`src/lib/feishu-app-credentials.ts`、`src/lib/server-secret-envelope.ts`
- 设置：`src/components/settings-panel.tsx`、`gmail-signature-settings.tsx`
- AI 邮件回复与模板：`src/components/email-detail.tsx`、`email-composer.tsx`、`ai-template-reply-composer.tsx`、`ai-reply-template-manager.tsx`
- 邮件正文隔离与编辑防护：`src/components/isolated-email-body.tsx`、`rich-email-editor.tsx`、`src/lib/isolated-email-document.ts`、`email-editor-html.ts`、`email-remote-content.ts`
- 账号与权限：`src/components/auth-provider.tsx`、`admin-accounts-panel.tsx`、`src/app/api/account/**`、`src/app/api/admin/accounts/route.ts`

### 账号管理与完全隔离

- 使用 Supabase Auth 登录，唯一主管理员由服务端 `APP_ADMIN_USER_ID` 指定；管理员不能在页面转让或新增管理员。
- 管理员可手动创建账号、填写或修改姓名备注、停用、恢复和重置临时密码；不发送邀请邮件，也不提供永久删除。
- 新账号及被重置密码的账号必须先修改临时密码；未完成改密或已停用账号不能访问业务数据。
- `account_profiles` 保存账号元数据，`user_data` 以 `user_id + data_key` 保存账号业务数据；设置、产品、红人开发及用户密钥均由 RLS 限制为当前正常账号。
- 管理员页面只读取账号元数据，不能读取成员密码、业务数据、Gmail 内容、飞书授权或密钥。
- Gmail、飞书、AI/YouTube 密钥与缓存均绑定当前账号；同一浏览器切换账号不会复用其他账号的资料或授权。
- 已移除固定每 30 分钟检查账号状态的轮询；继续使用 Supabase `persistSession` 和 `autoRefreshToken`，页面重新获得焦点或恢复可见时读取最新会话。
- 账号状态检查已区分明确停用/未开通/会话失效和临时网络或服务异常。只有明确会话失效时才刷新令牌并重试一次；临时异常保留同一账号已加载的工作台，不再自动当作账号停用退出。该机制降低误退出风险，但不等于永久登录。
- 账号管理依赖 `20260806_account_management.sql`、`20260807_account_admin_permissions.sql`、`SUPABASE_SECRET_KEY` 和 `APP_ADMIN_USER_ID`；Secret Key 仅允许服务端使用。
- 每位成员可配置自己企业的飞书自建应用。App Secret 使用服务端 `APP_SECRET_ENCRYPTION_KEY` 加密后保存在当前账号私密空间；管理员和其他成员不能读取。

## 2. 当前已实现能力

### 红人录入与飞书建档

- 支持批量识别 YouTube 频道、头像、近期长视频、公开邮箱、国家和推荐开发信语言。
- 飞书资源库/开发记录双表查重使用 60 秒快照和一次性索引；精确键冲突会阻止静默写入。
- “加入资源库”“新建开发记录”“快速建档”均先展示确认预览，确认后弹窗立即关闭，后台批量写入。
- 写入前按当前红人重新校验，其他红人的正常写入不再触发错误的“飞书数据已变化”提醒。
- 欧洲国家代码写入中文国家名；乌克兰等西里尔文本不会再因 `.com` 邮箱误判为葡萄牙语。
- 新建资源、开发记录和邮箱补写统一提取有效邮箱；飞书富文本对象不会再写成 `[object Object]`。
  服务端还会拦截任何包含该异常标记的待写字段。
- 邮箱候选会合并 YouTube 公开简介、资源库和开发历史记录，忽略大小写去重；来源冲突或多邮箱要求人工选择，手动填写/清空/选择后自动锁定，后续识别不会覆盖。
- 飞书写入必须使用用户保存的字段映射；不得硬编码字段名或自动修改表结构。

### 开发信、Follow Up 与邮件工作台

- Gmail 登录状态改为由应用级 Provider 统一维护，页面切换时不再由多个组件各自重复判断登录状态；短暂网络或会话检查失败不会把已确认连接误显示成“未登录”。
- Gmail 收件箱列表新增浏览器标签页内存缓存，按系统账号、Gmail 邮箱、邮箱视图/搜索条件和页码隔离；60 秒内返回 Gmail 页面时优先显示已有邮件并在后台同步，不先清空画面。
- Gmail 断开连接、切换系统账号或 Gmail 邮箱变化时会清理对应邮件列表与详情缓存，避免不同账号之间复用邮件内容；后台刷新失败时保留当前邮件并提供重试提示。
- 顶部“邮件生成进度”入口统一展示 Gmail/腾讯 AI 辅助回复、AI 模板回复、开发信、合作邮件和翻译任务；并发数可在 2 至 10 之间调节，超出并发限制的任务自动排队，完成后以现有弱提醒提供“打开”入口。
- 每项邮件任务独立显示真实业务阶段百分比：排队 0%、准备 10%、读取上下文 15%、生成正文 35%、整理结果 70%、翻译/分析 85%、成功 100%。运行中为蓝色，成功为绿色，失败为红色，中断为黄色；失败或中断保留最后进度，重试回到 0%，进度不会倒退。
- 邮件生成任务状态、必要的非敏感重试输入和已完成结果使用当前账号 `user_data` 中的 `email_generation_tasks` 保存，沿用既有 RLS；任务按系统账号、Gmail 邮箱、线程或红人隔离，默认保留 24 小时。
- 页面刷新或重新登录后可恢复已完成、失败和中断任务；上次仍为 `queued` 或 `running` 的任务会转为 `interrupted`，不会自动再次消耗 AI，只有用户点击“重试”才重新执行。
- 从邮箱列表重新打开同一邮件线程并点击同一种回复方式时，会按当前系统用户、邮箱账号、线程、回复依据和回复模式恢复 24 小时内最近一次有效任务；恢复内容包括外文正文、中文对照、回复想法、语言、语气及 AI 模板选择。
- 从“邮件生成进度”打开记录时，邮件详情会把被点击的 `taskId` 传给对应编辑器并精确恢复；若新来信改变了回复依据，普通打开不会自动套用旧正文，进度记录仍可切回原回复依据。
- 打开回复方式和自动邮件分析不会清空已恢复正文；同一编辑器内的未保存修改优先于迟到的云端恢复结果。“调整策略”保留上一版正文和原回复想法，只有用户明确重新生成时才创建新任务并替换同一任务键的旧记录。
- Gmail/腾讯 AI 辅助回复或 AI 模板起草成功保存草稿后，对应生成记录会显示“已保存邮件草稿”徽章；徽章绑定实际保存的任务来源和邮箱身份，正文、语言、语气、模板或回复依据改变后会清除，避免把新版本误标为已保存。功能上线前的历史草稿不反向补标。
- 当前阶段的 AI 请求仍由浏览器执行，因此关闭页面、刷新或标签页被系统回收时，生成中的请求可能中断；尚未实现“网页关闭后服务端继续生成”的后台队列。
- 恢复任务结果时不会覆盖用户已经手动修改的编辑器内容；云端快照禁止保存 Gmail Token、AI API Key 和无关完整邮件历史。云端同步失败时保留当前内存内容并明确提示，不假装已经保存成功。
- 首封开发信可按中文对照修改后重新翻译为目标语言；保存到所选 Gmail/腾讯草稿前仍需人工检查。
- 首封开发信生成后，标题、外文正文和中文对照默认可直接编辑，不再显示“编辑邮件”或“编辑正文”开关。只修改标题或外文可直接保存；修改中文后必须先根据中文更新外文，成功后才恢复保存草稿。
- 当前运营约定：首封开发信草稿保存成功后，用户立即前往对应邮箱检查和发送；系统不得自动发送。
- 一次/二次 Follow Up 按红人绑定邮箱读取真实 `SENT`、`DRAFT`、人工回复、自动回复和退信状态。
- 单条和批量按钮只在当前浏览器标签页后台生成目标语言正文与中文对照，不立即创建真实邮箱草稿或写飞书；结果按账号缓存 30 天，关闭/刷新标签页不保证任务继续。
- 审核窗口允许修改中文，必须点击“根据中文更新外文”后才能保存；批量结果逐封审核、逐封保存，不提供全部直接保存。
- 一次/二次 Follow Up 批量审核窗口已扩大到桌面宽屏布局，左侧待审邮件列表加宽，右侧外文、中文和操作区保持独立滚动；不改变生成、保存或写回逻辑。
- 已确认收到真人回信时显示黄色“已收到对方回信，无需跟进开发信”，并继续阻止后续开发信生成；退信、检查失败和缺少初次开发信仍保留异常语义。
- 用户明确点击“保存到所选邮箱草稿”前会再次检查回复、退信和重复跟进；草稿成功后按当前运营约定写回对应 Follow Up 状态和当天日期。邮箱草稿成功而飞书失败时只重试写回，不重复创建草稿。
- Gmail 与腾讯 AI 辅助回复、AI 模板起草只读取当前打开的真实会话：Gmail 按原生 `threadId`，腾讯按 `Message-ID` / `In-Reply-To` / `References` 严格关系隔离；服务端再次校验邮箱账号、目标邮件和上下文身份，发现混入其他线程时直接停止生成。
- AI 回复支持 SSE 流式正文；不支持流式时回退普通 AI 路由。快速切换腾讯邮件时只有最后一次会话请求可以更新页面，旧请求不能覆盖新邮件。
- Gmail 搜索直接使用 Gmail 全局查询，可搜索较早邮件；每日待办箭头可打开指定线程。
- Gmail 线程详情优先复用 5 分钟完整会话缓存。从待办打开时可先显示本轮读取的单封来信预览及已有中文，历史和附件继续加载；预览带 `isPartial`，不写入完整会话缓存，完整会话加载前禁用回复与转发。腾讯沿用相同预览边界。
- 每封带 `SENT` 标签的已发送邮件在看板中默认阻止 `http/https` 远程图片、背景、样式和 SVG 资源，避免 Mailsuite/Mailtrack 追踪像素把运营人员查看误记为红人打开；同线程的普通来信不受影响。
- `cid:`、`data:image/...` 和 `blob:` 内嵌图片继续显示，因此开发信内嵌产品主图不受影响。只有实际拦截到远程资源时才显示提示；人工点击“显示远程图片（可能触发邮件追踪）”只放开当前邮件，刷新、切换线程或切换账号后恢复保护。
- 该保护不修改 Gmail 邮件原文、历史追踪记录或 Mailsuite 数据，只能阻止本看板默认查看产生的新远程资源请求；用户主动放行、邮件服务器扫描、其他客户端和收件人打开仍可能被追踪。
- 每封真实 Gmail 邮件都可被选择为回复上下文锚点，即使没有飞书红人匹配；线程展示 From/To/Cc 参与者，自发邮件有多个候选收件人时要求人工选择，未确认收件人仍可生成但不能保存草稿。
- AI 回复的中文对照要求完整简体中文，并做中文比例和英文复制校验；外文正文与中文对照均支持流式显示。中文可编辑，确认后重新翻译目标语言。
- Gmail 红人画像分析在后台完成后不会自动覆盖当前草稿；用户可显式点击“结合画像优化草稿”，对比原稿与优化稿并人工采用，用户已写入的预算、日期和合作条件优先保留。
- Gmail/腾讯共用红人匹配会话缓存。匹配身份按系统用户、provider、`mailAccountId`、线程、标准化联系人邮箱及飞书配置隔离；翻译、已读状态、正文展示和普通重渲染不会触发重复搜索。重新打开同一邮件时先显示会话缓存，再按现有飞书短缓存静默核对；核对失败保留上一次正确结果。
- AI 辅助回复的“正在后台分析最近来往”提示位于回复想法输入框下方，分析开始或结束时不会再推动输入框位置。
- AI 同时生成外文和中文对照后可直接保存到当前邮箱草稿；点击“根据中文更新外文”后，中文和目标语言成为当前翻译依据，编辑器会在排队或翻译期间显示绿色“正在根据中文更新外文邮件”。
- 中文或目标语言随后变化时，必须重新翻译才能保存；外文可手动加入表情、调整措辞或排版，界面显示“外文已手动调整”，仍可直接保存草稿。
- 外文已人工润色后再次根据中文翻译会先提示可能覆盖当前外文；“直接发送”继续要求用户完成更严格的中文人工确认，不因放开草稿保存而扩大权限。
- AI 回复模板提供物流、折扣、常规合作和联盟邀请等内置规则，也支持当前账号独立的个人模板；模板管理使用编辑入口而非复制图标。
- AI 模板起草的“用自己的话补充事实”支持双模式输入：短内容可在紧凑文本框中直接输入，长内容可通过独立的“打开大编辑器”进入专注编辑；两处实时共享内容，大编辑器打开后聚焦到内容末尾，关闭不会丢失当前组件会话内的输入。
- AI 模板起草已收敛为“生成外文与中文对照 → 检查中文 → 必要时按中文更新外文 → 保存到当前邮箱草稿”；模板模式不再展示回复要点、缺失信息和风险审查区，普通“AI 辅助回复”分支不受影响。
- AI 回复模板管理弹窗使用自身可滚动内容区，标题和底部操作保持固定；在浏览器 100% 缩放且窗口高度不足时，可以滚动到“默认语气”并保存。
- Mailsuite/Mailtrack 等通知地址在联系人解析和 Gmail 草稿 API 两层拦截。
- Gmail 签名新增三选一范围：仅开发信、仅正常邮件、两者都生效；旧配置默认按两者都生效。
  AI 提示词明确禁止签名，程序还会按当前账号配置的签名内容移除 AI 末尾重复姓名/落款，再由统一渲染逻辑追加项目内签名。
- 合作项目的物流/折扣告知邮件支持中文对照编辑、重新润色翻译、后台生成，并使用用户确认的 Gmail/腾讯项目会话；
  只创建所选邮箱草稿，不自动发送；用户确认保存草稿成功后，自动勾选飞书中对应的“物流信息已告知”或“折扣信息已告知”。
  邮箱草稿成功而飞书同步失败时保留已创建的草稿，并提供仅重试飞书同步的入口，不重复创建草稿。
  发货日期在交给 AI 前统一转换为上海业务时区的 `YYYY-MM-DD`，不允许模型自行换算时间戳、时区或从物流编号推断日期。
  Gmail 选择回复锚点时排除表情回应和已有 `DRAFT`；腾讯使用严格标准关系头。当前页面内重复保存会更新原邮箱草稿；原草稿已被人工删除时，下一次用户确认保存才重新创建。

### 邮件正文隔离与编辑区防护

- Gmail 与腾讯通过同一 `EmailDetail` 接入 `IsolatedEmailBody`；清理后的 HTML 放入独立 `iframe srcDoc`，不再直接插入工作台 DOM。邮件的全局样式、字体、媒体查询只影响自己的正文；中文和纯文本继续安全文本显示，不修改翻译缓存或队列。
- 沙箱仅开放 `allow-same-origin allow-popups allow-popups-to-escape-sandbox`，禁止增加 `allow-scripts`。独立 CSP 禁止脚本、表单提交、连接、嵌套页面和对象；安全外链在新标签打开并带 `noopener noreferrer`，附件沿用原入口。
- 阅读区宽度受分栏限制，图片等比缩放，宽表格内部横向滚动。正文高度为 120–6000 CSS 像素，极长内容内部纵向滚动；图片、正文和栏宽变化通过每帧合并测量更新，尺寸未变化不更新。
- 邮件身份、正文或远程放行状态变化时创建独立正文实例，普通重渲染保持文档和选择；卸载清理监听，初始化失败只回退安全纯文本并提示，不回退到裸 HTML。
- 已发送邮件默认远程资源阻止继续有效，内嵌图片可用，人工放行仅作用于当前邮件。已设置无来源信息策略并验证普通图片和外链；但本机 Edge 对允许远程资源的 CSS 背景请求仍可能发送父页面来源，不承诺所有 CSS 请求都无 Referer。默认已发送邮件阻止测试通过，此限制不应被写成全面零追踪。
- 共享编辑器统一清理初始外部 HTML、粘贴和拖入 HTML：移除全局样式、危险标签/事件、越界定位和层级，保留安全文字、链接、图片、表格及行内格式；保留 `data-mail-inline-id` 和签名标记，避免损坏旧草稿内嵌附件。
- 编辑内容区限制尺寸和绘制范围，工具栏在边界外。清理复杂格式时显示“为保护工作台布局，已简化部分邮件格式”；仅打开已有草稿不触发更新回调或保存，实际输入继续沿用原回调。撤销/重做与模拟输入法事件已测，真实系统中文输入法仍待人工复核。

### 双邮箱与腾讯企业邮箱

- 已建立 Gmail / 腾讯企业邮箱通用账号模型，邮箱切换入口位于邮件页左侧栏；腾讯凭证只在服务端加密保存，API 每次按当前系统用户校验 `mailAccountId` 归属。
- 腾讯页面已覆盖写信、收件箱、未读、星标、已发送、草稿、搜索、分页、刷新、附件下载、邮件详情、回复、转发、AI 辅助回复、AI 模板回复、翻译、签名、草稿和人工确认发送。
- 腾讯发送通过 SMTP；发送前仍需用户确认，并保留延迟发送/倒计时取消。SMTP 请求真正提交后不能保证撤回。发送成功与“已发送”文件夹同步结果分开提示，避免同步失败诱导重复发送。
- 腾讯草稿更新采用“先保存新版本，再删除旧版本”；旧版本删除失败时保留成功结果并明确警告。Gmail 草稿 ID 与腾讯文件夹/UID 不共用。
- Gmail 与腾讯已有草稿均使用完整收件人、抄送、密送、富文本、普通附件和 `cid:` 内嵌图片编辑上下文。任一原始附件或内嵌图片加载失败时禁止覆盖原草稿；编辑已有草稿不会自动重复追加签名。
- Gmail 与腾讯邮件时间统一优先采用可验证的邮件头时间，并使用服务端内部时间等安全来源回退；异常或未来日期不会再制造错误时间，列表和会话按解析后的真实时间稳定排序。
- 腾讯会话只使用 `Message-ID`、`In-Reply-To` 和 `References` 建立关系，不再凭主题、联系人或时间接近模糊合并。缺失或损坏标准关系头时保持独立，避免同一红人的不同项目串线。
- 腾讯会话加载已改为：完整读取当前邮件 → 轻量读取候选关系头 → 只批量读取确认属于会话的正文。候选关系判断不下载附件；同请求去重、并发请求合并，并使用按系统用户和邮箱账号隔离的 60 秒短缓存。手动刷新绕过缓存，草稿/发送/标记变更后使缓存失效。
- 合作项目邮件优先使用项目邮箱绑定，其次使用红人邮箱绑定；没有有效绑定时要求人工选择。项目首次回复还必须选择具体会话或“新建独立邮件”，项目会话绑定不会覆盖红人全局绑定。
- 首封开发信、Follow Up、物流、折扣和合作回复已接入邮箱身份；草稿、AI 任务、翻译结果、联系人历史和顶部生成进度均带 provider 与 `mailAccountId`，避免 Gmail/腾讯或不同账号互相覆盖。
- 当前运营规则保持不变：首封、Follow Up、物流或折扣邮件在用户确认并成功保存到对应邮箱草稿后，即按既有飞书字段映射标记“已发/已告知”；草稿保存失败时不得更新飞书，飞书写回失败时不得重复创建草稿。
- Supabase 私有桶 `mail-attachments-temp` 已由用户在生产控制台创建为非公开、单文件 25MB，并执行按 `auth.uid()` 路径隔离的上传/读取/删除策略；对应迁移文件已包含在 `1.2.0` 提交中。附件下载已由用户在本地确认可用，大附件发送与双用户越权测试仍待真实验收。

### 合作项目

- 读取飞书“详细合作记录表”，提供列表、看板、日历、搜索、风险和阶段多选筛选。
- 七阶段由日期与业务字段自动计算；视频已发布等已完成合作不再显示阶段停留天数。
- 阶段日期统一显示为中文年月日；发货、到货、拍摄完成和实际上线日期可在详情中修改。
- “物流信息已告知”“折扣信息已告知”可勾选或撤销；日期和复选框均在后台静默写回，成功/失败提示。
- 频道头像优先由映射的频道链接解析 Channel ID 后调用 YouTube API，列表缓存结果并回退字母头像。
- 合作项目与红人列表切换使用缓存和显式刷新，减少重复读取飞书。

### 每日待办与工作日历

- 手动任务支持标题、放大描述、优先级、默认当日截止日期、截止时间、编辑、删除、完成和恢复；高密度行布局可在一屏显示更多任务。
- 待办分为“待完成、今日已完成、历史已完成”三组，三组均可折叠；已完成任务可以恢复，未手动完成的任务持续保留。
- 每日待办并行读取当前可用的 Gmail 授权账号和所有已连接腾讯企业邮箱；每个邮箱独立记录实时、缓存、失败或断开状态，一个邮箱失败不会清空其他邮箱的实时结果。
- 每日待办识别挂在应用主壳，不要求停留在待办页。登录正常、无需改密、设置和邮箱账号加载完成后，网页可见时按新鲜度检查；定时检查间隔为 5 分钟，恢复可见时按新鲜度补查，恢复联网或点击“刷新来信”可强制刷新。网页关闭后不发现新邮件；后台标签页不保证即时检查。缓存待办可先展示，但没有本轮有效正文快照时不能仅凭中文摘要预翻译。
- Gmail 查询近 72 小时活跃线程并沿用原生线程关系；腾讯只根据标准回复关系头判断我方是否已经回复，相同主题但没有 `In-Reply-To` / `References` 的邮件不会被误判为已回复。
- 腾讯待办与详情使用一致的正文规范化：常规邮件只读正文分段，复杂 MIME、多段正文、flowed 或没有有效分段编号时回退完整 MIME 解析，可能连同原始邮件附件字节读取；不主动下载独立附件、不创建附件预览或上传附件。不能再把 12000 字截断内容当作完整翻译依据。AI 摘要失败不影响任务本身显示。
- 任务键和完成状态按 provider、`mailAccountId` 及真实线程/文件夹/UID 隔离；旧版无来源任务只在能够唯一匹配 Gmail 任务时迁移，不用主题或正文相似度跨账号合并。
- 每条任务只显示 Gmail / 腾讯企业邮箱来源卡片，不在任务行重复显示收信邮箱地址；顶部邮箱读取状态仍显示具体账号。点击 Gmail 任务会切换到对应 Gmail 线程；点击腾讯任务会切换到对应账号、文件夹和 UID。原邮件移动时只在原账号内按 RFC `Message-ID` 重定位，账号断开或仍找不到时提示错误，不打开其他相似邮件。
- 每次刷新完成红人匹配后，近 72 小时内识别出的来信进入统一预翻译队列，包括已完成与未完成待办，不受已读状态影响；继续排除自动回复、退信、无有效正文和最新来信后已有我方回复的邮件，不扩展到整个收件箱。
- 后台最多 2 个翻译请求，按新到旧排队；前台独立 1 个槽，总并发最多 3。相同来源邮件及完整原文复用请求，缓存合并写入；自动失败重试间隔为 5 秒、30 秒、2 分钟，三次重试后停止。恢复联网、重新可见或人工重试可重新尝试，其他缓存更新不会无限重启失败任务。
- 待办“中文已备好”只来自同系统账号、同邮箱/provider、同 messageId、同规范化完整原文的有效中文缓存；中文摘要和队列完成本身不能代表译文可用。正文变化或新来信不能套用旧结果；旧 Gmail 缓存只有同身份、同原文且有效时才迁移。
- Gmail 待办与详情共用 `readGmailMessageBody`；腾讯共用 `resolveMailTranslationBody`。只统一换行、行尾空白等格式，不删真实内容。缓存以完整原文校验，实际优先翻译当前来信正文，引用历史仍按现有独立入口处理。
- 待办打开请求携带目标邮件和 `autoShowTranslation`，默认展示有效中文；如果仍在翻译则复用请求，完成后自动展示；失败保留原文并明确提供重试。普通收件箱打开仍默认原文，点击翻译时直接使用有效缓存。用户主动切回原文后，后台完成不会强行切回中文。
- 本轮已读取的来信快照可用于待办详情预览；没有快照或译文时仍可能需要读取/翻译时间，“中文已备好”不代表历史和附件已加载。腾讯打开请求还绑定 `mailAccountId`，账号变化后不能执行旧账号导航。
- 新外部来信晚于旧任务完成时间时可重新进入待完成。勾选和恢复不修改邮箱或飞书数据；邮件正文成功显示后，Gmail 和腾讯会将当前真实会话标为已读。列表状态统一为：未选中已读白底；未选中未读淡蓝底、粗体和未读标记；选中已读白底加左侧蓝线；选中未读同时保留淡蓝底、粗体、未读标记和左侧蓝线。
- 工作日历同时展示手动日程、未完成待办和飞书合作项目六类只读节点：确认合作、发货、到货、拍摄完成、预计上线、实际上线。
- 合作节点按需读取，普通进入复用 60 秒飞书缓存，支持手动刷新；失败只提示错误，不影响原有日程和待办。
- 月历单格最多展示 2 条合作节点，超出显示“还有 N 项”；预计上线已过期且缺少实际上线日期时显示红色。
- 点击日期只打开当天只读详情，不会自动创建日程；独立“新增日程”按钮位于“刷新项目”左侧。合作节点显示频道头像。
- 点击合作节点可切换到合作项目并自动打开对应详情；工作日历不修改合作节点，也不改变原有飞书写回流程。
- 日期构造使用本地日期工具，避免 UTC 转换导致工作日历偏移一天。
- 月历选中日期的圆形标记按日期单元格水平居中，避免视觉偏位。

### 导航与红人录入边界

- 原“AI 提示词”导航已改名为“AI 系统功能提示词”，与“AI 起草邮件提示词”明确区分职责。
- 顶部旧“添加红人”按钮、旧弹窗和红人数量角标已经移除，避免产生一条与飞书主数据不同步的本地录入路径。
- “红人列表”继续直接展示飞书多维表格；新增和管理红人应在飞书多维表格中完成。

### 版本更新提醒与网页图标

- 更新弹窗和设置页统一读取 `src/lib/app-release.ts`，当前源码版本为 `1.2.5`“来信中文预翻译优化”（2026-09-02）。两条用户确认文案为“优化 Gmail 与腾讯企业邮箱的来信预翻译，每日待办识别到来信后，自动提前准备中文。”和“更新醒目的网页缩略图图标”。`package.json` 的 0.1.0 不是用户界面版本。
- 登录后的更新提醒按“系统账号 + 浏览器 + 版本”记录已显示状态；用户跳过多个版本时只显示一个提醒，其中汇总尚未看过的更新，不会连续弹出多个窗口。
- 设置页“关于红人推广看板”读取同一份版本信息。更新提醒不修改 Gmail、飞书、账号授权或业务数据；换浏览器、换电脑或清除浏览器数据后再次显示属于当前设计。

- 浏览器图标由 `src/app/favicon.ico` 和 `src/app/icon.png` 提供；当前文件已被 Git 跟踪，沿用 Next.js 文件式图标入口。浏览器/favicon 缓存和构建产物可能晚于源码更新，应独立核对，不以版本提示代替图标验收。

## 3. 外部写入与数据边界

- 不自动发送 Gmail 或腾讯邮件，不自动群发开发信。腾讯直接发送已经存在，但必须由用户检查内容并明确点击确认。
- Gmail/腾讯草稿、邮件发送和飞书创建/更新仍由用户明确点击确认；后台执行只改变等待体验，不扩大授权。
- 不自动清理历史 Gmail/腾讯草稿或飞书异常数据。现有 `[object Object]` 邮箱记录如需批量修复，
  应另做“只读扫描 → 变更预览 → 用户确认 → 批量写回”。
- YouTube API 只能读取公开频道资料；隐藏邮箱不可获取，邮箱只能从公开内容提取或人工填写。
- AI 模型由用户配置 OpenAI-compatible 接口；项目不内置 DeepSeek，也不能假设任意代理地址兼容。
- 设置页可按提供商自动填充常用 API 地址和推荐模型，并提供三步指引与连接测试；自定义兼容接口仍需用户核对真实 Base URL/完整 endpoint 和模型名。
- AI“测试连接”使用独立的 `testConnection` 动作，只发送最小模型请求并要求系统登录、当前账号私有密钥和模型配置；它不再伪装成 `draft`，也不读取或伪造 Gmail/腾讯会话。正式 `analyze`、`draft`、`optimizeDraft`、`templateDraft` 仍必须通过真实邮箱账号归属和会话上下文校验。
- `APP_SECRET_ENCRYPTION_KEY` 只允许保存在服务端并保持稳定；更换后既有成员的飞书 App Secret 无法解密，需要逐人重新配置。

## 4. 当前证据与验证状态

| 事实面 | 状态 | 证据与限制 |
| --- | --- | --- |
| 代码 | verified-current | 2026-09-02：HEAD、本地 origin/main 与 GitHub 直接读取的 main 同为 `5151fcb3117b09152b076e22a76577d1b3381717`；含正文隔离、编辑防护及测试，用户界面版本仍为 1.2.5 |
| TypeScript | verified-current | 最近一次实现验证的 `tsc -p tsconfig.json --noEmit` 通过；本次收尾仅改文档 |
| 逻辑测试 | verified-current | 最近一次完整 `tests/run-tests.ts` 280/280 通过，含新增隔离文档 6 项测试 |
| ESLint | verified-current | 最近一次 src/tests 0 error，7 个既有 warning：email-detail 的 1 个 img 提示，reminder-panel 的 6 个未使用图标；没有降低规则 |
| 生产构建 | verified-current | `mail-isolation-final-build` 隔离快照 webpack 生产构建通过，41 个生成项；6 个核心修改源码的 SHA256 与当前提交文件一致。未覆盖仓库旧 .next，本次文档收尾没有重新构建 |
| 本地运行态 | verified-current | 本轮 localhost 和 127.0.0.1 的 5000、5001 均连接拒绝；现有 .next BUILD_ID 仍为 `iUYYNCQTJkBCWsMbDLI2A`，旧构建版本提示为 1.2.4。本轮未停止或重启服务 |
| 隔离浏览器测试 | verified-current | 双邮箱完整应用模拟在第一份隔离构建上通过；最终源码另通过 17 类组件浏览器用例及内嵌附件标记保留断言。最终完整应用构建未重新跑整套浏览器流程；详细尺寸与限制见下，不把组件验证冒充全应用最终快照验收 |
| 真实业务验收 | pending | 用户已确认此前功能做过真实验收；新增正文隔离的真实邮件、真实系统中文输入法、其他浏览器及移动端仍待人工复核。预翻译在正式域名的真实体验也需用户确认 |
| GitHub / Vercel | pending | 直接远端已核实 5151fcb，GitHub Vercel check 为 success，更新时间 2026-09-02T10:33:46Z；正式域名公开页返回 200，但发布指向及登录态未核实，不能标为线上验收通过 |
| 文档 | changed-and-verified | 本轮 README 与本文件同步当前机制、版本及证据边界；修改尚未提交 |
| 规则 | verified-current | 已完整读取全局和项目 AGENTS；未发现所修改文档路径下的覆盖规则，无需改写规则。历史入口均已明确停用并指向本文件 |
| 记忆 | out-of-scope | 无独立获准的记忆写入入口；宿主生成记忆只读，没有手改生成文件 |
| 工作区 / 清场 | pending | 安全路径内业务代码与测试无未提交差异；本轮仅新增两份文档差异。受保护现场未进入核验，复核证据与运行残留全部保留，不宣称整个仓库干净 |

Vercel 检查链接：
https://vercel.com/daniel-wu-s-projects/influencer-kanban-main/As2YtEUxLxrsXcsXX4TnzXTsPjKG

正式域名为 `https://kolworkflow.vercel.app/`。本轮 PowerShell 访问发生 SSL 连接错误，改用 bundled Node 只读访问返回 200，标题为“红人推广看板 | 跨境电商工作台”；只确认公开页可达。没有更改证书、网络或部署配置。

本机隔离测试证据保留在：
`C:/Users/Admin/.codex/visualizations/2026/08/30/01a051d6-8669-7db2-b32a-fe23dcc1d5d8/`

- `mail-isolation-qa.cjs`：双邮箱完整应用模拟；固定 1880×932 窗口，Gmail/腾讯各 8 行，原文→中文→原文时，修复前行高为 77→81→77px，修复后为 81→81→81px；栏宽 459px 稳定，导航和按钮测量位移 0px，切换新增翻译请求 0。没有通过修改列表行高掩盖问题。
- `mail-layout-{gmail,tencent}-{original,chinese}.png` 为修复前截图；`mail-isolation-{gmail,tencent}-{original,chinese,stress,editor}.png` 为修复后截图。完整应用模拟还覆盖展开/折叠、拖动分栏后切换、超宽表格、极长正文及转发/危险 HTML 粘贴；页面和控制台异常、真实外部写入均为 0。
- `mail-isolation-harness-qa.cjs`、`mail-isolation-harness.tsx` 和 `mail-isolation-editor-harness.png`：最终组件源码验证，覆盖旧草稿仅打开不写回、清理幂等、撤销重做、模拟输入法、HTML 拖入、图片样式与尺寸、文档稳定与选择、已发送远程阻止/放行、内嵌图片、图片/链接无 Referer、账号切换、脚本/表单阻止、无 opener 外链和纯文本兜底。8 条安全控制台消息是主动注入脚本/表单的预期拦截；CSS 背景 Referer 限制见第 2 节。
- `mail-isolation-build/` 是完整应用浏览器测试使用的较早快照；`mail-isolation-final-build/` 是最终生产构建快照。两者不是仓库的当前运行服务；脚本 `mail-isolation-build-run.cjs` 不复制或打印密钥。
- `mail-translation-verification.md`、`mail-translation-qa.cjs`、`mail-qa-report.json` 和 `mail-qa-{todos,gmail,tencent}.png` 保留此前预翻译 15 项模拟流程证据。其中 b2cf601、未提交等 Git 描述是历史快照，当前状态以本文件为准；4 条 503 为主动注入失败测试。
- 开发模式曾有 AI 助手悬浮按钮 hydration 警告，本次不改无关组件，也未将其宣布修复。所有自动验收使用模拟数据，没有创建真实草稿、发送邮件或写飞书。

## 5. 下一轮验收与诊断入口

### 邮件正文隔离优先复核

1. 确认运行的是包含 5151fcb 的构建，再分别在 Gmail/腾讯打开普通邮件、营销邮件、超宽表格和长正文。
2. 固定窗口和分栏，原文→中文→原文、展开/折叠、切换线程及打开回复助手，检查列表行高、栏宽、导航和工具栏位移不超过 1 CSS 像素；再拖动分栏复测。邮件正文可以滚动，不能影响工作台其他区域。
3. 在用户授权的转发/旧草稿编辑界面测试危险格式粘贴、真实中文输入法、撤销/重做及工具栏；仅打开不能保存。不要为了验证创建真实测试草稿或发送邮件。
4. 检查已发送远程资源默认阻止、内嵌图正常、外链新标签以及账号切换；若出问题，先查共享隔离正文、HTML 清理和编辑器，不调整邮件列表尺寸掩盖根因。

### 预翻译与业务回归

1. 先核对实际运行的是哪个构建。源码版本、构建版本、远端提交、Vercel 检查和正式域名各自验证；需要重启本地服务、构建或部署时按用户当次授权执行。
2. Gmail/腾讯各准备一封近 72 小时、已匹配红人且尚未回复的来信。停留在普通邮箱等其他页面，确认自动发现，无需先打开每日待办；也可人工“刷新来信”立即检查。
3. 待办显示“中文已备好”后进入，应先看到中文，历史/附件可能继续加载；完整会话未返回前回复与转发保持禁用，返回后恢复。
4. 从普通邮箱手动打开同一邮件，先显示原文；点击翻译应命中已有中文。再次切原文后，后台刷新不应抢回中文。
5. 翻译仍在运行时打开应复用同一请求；失败显示真实错误并保留原文，可人工重试。完成待办后重开仍复用有效缓存。
6. 验证正文变更、新 messageId 与切换账号：重新翻译真实新内容，不复用其他账号/邮件结果。
7. 如果再次出现“已备好但要重译”，先比对待办与详情的账号、messageId、规范化完整原文和缓存内容；区分等待历史读取与等待翻译，不能只改状态文案掩盖问题。
8. 真实草稿、发送、飞书写回、大附件发送和双用户越权测试由用户明确操作；本轮模拟测试没有替代这些验收。

## 6. 工作区与只读清场预览

- 收尾前安全路径（src、tests、docs、README、AGENTS、public、assets、package/lock）无未提交差异；本轮只改 README 和 docs/HANDOFF.md。
- 最近 5 个提交：`5151fcb` 更新优化小bug、`c8934e4` 1.2.5、`b2cf601` 小细节优化、`203aa1f` 修复了翻译邮件的Bug、`0200672` 1.2.4。
- 禁止触碰、恢复、删除、修改权限或清理：
  - `C:/Users/Admin/Documents/Codex/influencer-kanban-main/.codex-tmp/workflow-share-deck/slides-test-temp`
  - `C:/Users/Admin/.codex/worktrees/347c/influencer-kanban-main`
  - `C:/Users/Admin/.codex/worktrees/5a94/influencer-kanban-main`
- 宽范围 Git status 即使排除 .codex-tmp，仍曾提示其中两个目录 Permission denied。此后改用明确安全路径；未读取其中内容，无法区分真实删除与权限不可见，不能尝试修复或宣称已清理。
- 历史提交 `bbb9584` 的 PPT 产物及旧文档删除保持原状，不重写历史。两个外部 worktree 仍视为可能含唯一修改，本轮不进入、不审内容、不清场。
- 根目录开发服务日志、pnpm 压缩包、package/pnpm-js/pnpm-win 解包目录是历史运行残留候选，未逐个证明可安全删除；node_modules、.next 和环境文件不是本次清理对象。
- 隔离浏览器脚本、报告、截图和两份隔离构建是修复证据，全部保留。隔离构建的 node_modules 为指向仓库依赖的目录联接，不得递归跟随联接清理，以免误删共享依赖。
- 没有删除任何文件、停止任何服务、修改权限或编辑项目外证据。复核现场仍保留，只有用户看完报告后另行明确授权，才能讨论清场；受保护路径不因一般清场授权而解禁。

## 7. 常用验证命令

优先使用项目现有依赖，不运行自动重装。本机命令：

```powershell
& 'C:\Users\Admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' node_modules/typescript/bin/tsc -p tsconfig.json --noEmit
& 'C:\Users\Admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' node_modules/eslint/bin/eslint.js src tests
& 'C:\Users\Admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin\node.exe' -e "process.geteuid=()=>0; require('tsx/cjs'); require('./tests/run-tests.ts')"
& 'C:\Users\Admin\.cache\codex-runtimes\codex-primary-runtime\dependencies\native\git\cmd\git.exe' status --short -- src tests docs README.md AGENTS.md public assets package.json pnpm-lock.yaml
```

测试启动中的 `process.geteuid` 替代仅限该 Node 测试进程，用于绕过本机 tsx 的 `uv_os_get_passwd ENOMEM`；不改系统权限、项目代码或依赖。普通环境可直接运行 `node --import tsx tests/run-tests.ts`。

生产构建使用 bundled Node 执行 `node_modules/next/dist/bin/next build --webpack`；它会写入 .next，不要在服务使用该目录时未经确认覆盖。上一轮使用项目外隔离快照验证，最终目录见第 4 节；本轮文档收尾没有重新构建或启停服务。

## 8. 后续需求（未作为本轮实现承诺）

- 合作产品多选筛选：历史交接记录指出产品数组被压为组合字符串；新任务应先核实当前实现，再考虑“包含任一”的多选筛选，避免 P210 与 P210-B 错配。
- AI 提取飞书可写回信息或合作阶段变化后，给出变更预览和建议；用户确认后才执行，不自动改飞书。
- 如果要求关闭网页后仍即时发现并翻译新邮件，需要另行讨论服务端任务、授权及部署方案；不属于当前浏览器内 5 分钟检查机制。

## 9. 新对话开场提示词

为避免复制一份很快过时的完整状态，使用短提示词并以本文件的实时核对结果为准：

```text
请接手项目：
C:\Users\Admin\Documents\Codex\influencer-kanban-main

我是没有编程经验的用户，请全程用简单、清晰的中文沟通。
先完整阅读根目录 AGENTS.md 和 docs/HANDOFF.md，再只读核对当前分支、HEAD、origin/main、最近 5 个提交、安全路径下未提交改动及新需求相关完整 diff。

交接快照：2026-09-02，main / 5151fcb3117b09152b076e22a76577d1b3381717，源码版本仍为 1.2.5「来信中文预翻译优化」。GitHub main 已直接核实一致，Vercel check success、正式域名公开页 200，不代表发布指向或登录后功能已验收。本地 5000/5001 均连接拒绝，仓库 .next 仍是旧构建，不能直接启动后当作当前版本验收。
README 和 docs/HANDOFF.md 有本次收尾产生的未提交文档修改，必须保留。后续实际状态可能变化，请重新核对。

最新修复：Gmail/腾讯共用 iframe 邮件正文隔离；共享编辑器在旧草稿、转发、粘贴/拖入 HTML 前清理危险格式。保留正文外观，不能让邮件样式影响工作台；沙箱绝不开放脚本，仅打开草稿不写回。
验证：TypeScript、280/280 逻辑测试、ESLint 0 error/7 既有 warning、最终隔离生产构建 41 个生成项通过；双邮箱模拟布局及最终组件测试有截图和脚本。完整应用模拟使用较早隔离快照，真实邮件、真实中文输入法和跨浏览器仍待验收；远程 CSS 背景 Referer 限制详见交接文档，不要宣称全面零追踪。
预翻译机制保持不变：全局主壳可见时按 5 分钟周期识别，不需进入每日待办；待办打开默认中文，普通邮箱打开默认原文、点击翻译命中缓存。来信预览与历史/附件加载分开，完整会话加载前不能回复或转发。

不自动创建真实测试草稿、不发送邮件、不写飞书；保留用户明确确认。保持 Gmail/腾讯、系统账号、邮箱账号、项目会话和任务缓存隔离，服务端核验 mailAccountId 归属。项目邮箱绑定优先于红人绑定，项目会话不能覆盖红人全局绑定。
不安装依赖，不擅自改数据库、RLS、权限、密钥、环境变量或部署配置。未经我的明确授权，不提交、不推送、不部署、不清理、不启停服务，也不覆盖未提交差异。测试证据和隔离构建继续保留。

禁止触碰：
C:\Users\Admin\Documents\Codex\influencer-kanban-main\.codex-tmp\workflow-share-deck\slides-test-temp
C:\Users\Admin\.codex\worktrees\347c\influencer-kanban-main
C:\Users\Admin\.codex\worktrees\5a94\influencer-kanban-main
不要重写 bbb9584。只使用明确安全路径做 Git 状态检查，避免扫描受保护目录。

先根据我的新需求说明业务目标、现状/根因、风险、准备修改的文件、最小方案和明确不改的内容。若只要求分析，先不修改。
本次新需求：
【在这里填写需求、现象、复现步骤和预期结果，并附截图】
```
