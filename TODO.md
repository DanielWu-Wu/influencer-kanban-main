# TODO

## 已完成

- 红人开发台重构为 3 个流程 Tab：红人录入、邀约确认、开发信。
- 支持红人资源库和红人开发记录表两套飞书配置。
- 新增 `creator_prospects` Supabase 本地表和迁移文件。
- YouTube 频道识别支持基础资料、头像、公开邮箱、最近长视频和视频指标。
- 最近长视频标题默认翻译成中文。
- 点击“识别频道”后自动进行飞书双表查重。
- 红人录入列表支持直接填写邮箱，并用于双表写入。
- 国家/语言显示尽量中文化，减少裸代码。
- 资源库疑似收录支持 Hover 预览飞书资源库记录。
- “确认可建”按钮改为“确认为新红人”。
- 新建开发记录时，兜底补全/追加资源库邮箱。
- 写入红人资源库时备注默认简化为 `来源：红人开发台`，并支持确认前自定义修改。
- 写入资源库时支持内容类型字段，自动根据飞书选项做初步推断。
- 邀约确认页增加联系人姓名和开发信语言。
- AI 可识别联系人姓名和开发信语言。
- 产品数据库简化为产品资料卡。
- 支持产品主图上传、预览和开发信插入。
- 开发信支持 3 个备选标题及中文翻译。
- 开发信标题和正文支持分别重新生成。
- 开发信预览支持产品型号链接、主图和签名链接。
- 保存 Gmail 草稿后自动移出开发信队列。
- 保存 Gmail 草稿后自动标记飞书双表“初次开发信 = 已发”。
- Gmail 只创建草稿，不自动发送。
- Gmail 翻译和 AI 回复做过第一轮提速。
- Gmail 红人频道头像支持本地缓存和列表预取。
- AI 助手入口改为可拖动圆形悬浮球。

## 当前优先级最高的下一步

1. **实测红人开发台关键流程**
   - 识别频道。
   - 自动飞书查重。
   - Hover 查看“资源库疑似收录”。
   - 选择“确认为新红人”或“关联资源记录”。
   - 新建开发记录。
   - 邀约确认。
   - 生成开发信。
   - 保存 Gmail 草稿。
   - 检查飞书双表“初次开发信”是否自动变为“已发”。

2. **完善失败提示**
   - Gmail 创建草稿失败时，尽量展示 Gmail API 原始错误原因。
   - 飞书写回失败时，提示是资源库失败、开发记录表失败、字段映射缺失，还是权限/连接问题。

3. **给“开发记录疑似重复”增加类似 Hover 预览**
   - 当前只给“资源库疑似收录”做了 Hover。
   - 如果开发记录表也疑似重复，用户同样需要不离开页面判断是否关联旧开发记录。

4. **继续清理残留乱码文案**
   - 只在相关文件被修改时顺手清理。
   - 不做大规模无关重构。

## 第二阶段可以做的优化

- Gmail AI 回复继续提速：
  - 更细的邮件正文/引用拆分。
  - 更强的缓存策略。
  - 可选深度分析更多历史邮件。
- YouTube 头像服务器缓存：
  - 可新增 Supabase `youtube_channel_cache` 表。
  - 需要用户明确确认数据库结构变更后再做。
- 飞书字段映射健康检查：
  - 设置页显示哪些流程依赖哪些字段。
  - 缺字段时告诉用户会影响哪一步。
- 开发信生成质量优化：
  - 更明确区分产品事实、合作建议、不能编造的参数。
  - 针对不同语言/国家的开发信风格模板。
- 产品资料管理第二版：
  - 支持多个产品素材。
  - 支持按国家/站点保存不同产品链接。
  - 但不要过早做完整素材库。
- Gmail 草稿记录重试：
  - 若 Gmail 成功但飞书“已发”标记失败，可做一个明确的重试入口。
  - 第一版暂时只提示失败原因。

## 暂时不做 / 不要过度开发

- 不做开发信质检面板。
- 不做自动发送 Gmail 邮件。
- 不做自动群发开发信。
- 不做复杂产品素材库。
- 不做服务器头像缓存，除非明确确认数据库变更。
- 不做新框架、新状态管理库或大规模 UI 重构。
- 不做过度动效和营销风页面。
- 不把 AI 变成自动决策者；AI 只辅助，关键业务动作仍由用户控制。

## 已知问题和技术债

- 某些源文件中可能仍有历史乱码文案，需要逐步清理。
- Gmail 草稿创建偶尔失败后重试成功，需继续收集错误原因。
- Feishu OAuth 权限和字段映射缺失时，错误提示仍可更清晰。
- YouTube API 额度有限，批量头像/频道解析要控制请求量。
- 产品主图使用现有字段保存压缩预览，长期不适合大型素材管理。
- 当前部分状态仍依赖本地/云设置混合存储，后续多人协作可能需要更完整的数据模型。
- `docs/AI_HANDOFF.md` 与 `PROJECT_HANDOFF.md` 未来可能重复，需要定期同步重点信息。
